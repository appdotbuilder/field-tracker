import { type PoiTask } from '../schema';

export async function getUserPoiTasks(userId: number): Promise<PoiTask[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all POI tasks assigned to a specific user.
    // Should include POI information for display and navigation purposes
    return Promise.resolve([]);
}